<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    >

    <data>

    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/black">

        <TextView
            android:id="@+id/tv_success_snackbar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="15dp"
            android:layout_marginTop="12dp"
            android:layout_marginBottom="14dp"
            android:includeFontPadding="false"
            android:text="@string/tofolder"
            android:textColor="@color/white"
            android:theme="@style/TextAppearance.body01"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent"/>

        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_success_snackbar_scrapbook"
            android:layout_width="wrap_content"
            android:layout_height="38dp"
            android:layout_marginEnd="15dp"
            android:paddingStart="0dp"
            android:paddingEnd="0dp"
            android:background="@color/black"
            android:includeFontPadding="false"
            android:paddingHorizontal="10dp"
            android:text="@string/btn_scrapbook"
            android:textColor="@color/main_color"
            android:theme="@style/TextAppearance.headline01"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="parent" />


    </androidx.constraintlayout.widget.ConstraintLayout>

</layout>